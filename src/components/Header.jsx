import React from 'react';

funtion Header({ title }) {
    return (
        <Header className='header'>
            <h1 className='title'>{title}</h1>
        </Header>
    );
}

export default Header;